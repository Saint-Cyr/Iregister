<td>
    <a href="#" target="_blank">
        {% if object.image is empty and object.collectedImage is empty %}
            {% if object.sexe == "F" %}
                <img src="{{ asset('images/avatar2.png') }}" alt="{{ object.name }}" style="width: 85px; height: 65px; border-radius: 50%"/>
            {% else %}
                <img src="{{ asset('images/avatar.png') }}" alt="{{ object.name }}" style="width: 85px; height: 65px; border-radius: 50%"/>
            {% endif %}
        {% else %}
            <img src="{{ asset('upload/person/media/'~object.collectedImage) }}" alt="{{ object.name }}" style="width: 85px; height: 65px; border-radius: 50%"/>
        {% endif %}
    </a>
</td>